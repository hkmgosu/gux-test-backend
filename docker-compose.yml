version: '3.8'
services:
  db:
    image: postgres:13
    environment:
      POSTGRES_USER: tu_usuario
      POSTGRES_PASSWORD: tu_password
      POSTGRES_DB: task_manager_db
    ports:
      - '5432:5432'
